const formGroup = this.headersList.controls[index] as FormGroup;
Immediately return this expression instead of assigning it to the temporary variable "formGroup".

 const formGroup = this.apisList.controls[index] as FormGroup;
